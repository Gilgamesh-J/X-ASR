<Window x:Class="VibeXASR.Windows.Ui.Wpf.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vibe XASR" Height="640" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="#15151B"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <ResourceDictionary Source="Styles.xaml"/>
    </Window.Resources>

    <DockPanel>
        <!-- header -->
        <Border DockPanel.Dock="Top" Background="{StaticResource Surface2}" Height="58">
            <Border.Effect><DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.35" Color="#000000"/></Border.Effect>
            <Grid Margin="18,0,16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="26" Height="26" CornerRadius="7" Background="{StaticResource AccentGrad}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Rectangle Width="2.4" Height="8"  Fill="White" RadiusX="1.2" RadiusY="1.2" Margin="1,0"/>
                            <Rectangle Width="2.4" Height="13" Fill="White" RadiusX="1.2" RadiusY="1.2" Margin="1,0"/>
                            <Rectangle Width="2.4" Height="9"  Fill="White" RadiusX="1.2" RadiusY="1.2" Margin="1,0"/>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="TitleText" Foreground="{StaticResource Text}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="11,0,0,0"/>
                    <TextBlock x:Name="CountText" Foreground="{StaticResource TextMuted}" FontSize="11" FontFamily="Cascadia Mono, Consolas" VerticalAlignment="Center" Margin="10,2,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="ExportBtn" Style="{StaticResource Ghost}" Content="导出"/>
                    <Button x:Name="ClearBtn" Style="{StaticResource Ghost}" Content="清空" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- privacy banner -->
        <Border DockPanel.Dock="Top" Background="{StaticResource AccentSoft}" Height="46">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                <TextBlock Text="🔒" FontSize="14" VerticalAlignment="Center" Foreground="{StaticResource Success}"/>
                <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="PrivacyTitle" Foreground="{StaticResource Success}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock x:Name="PrivacySub" Foreground="{StaticResource TextMuted}" FontSize="10.5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- optional stats bar -->
        <Border x:Name="StatsBar" DockPanel.Dock="Top" Background="#1A7C5CFF" Height="32" Visibility="Collapsed">
            <TextBlock x:Name="StatsText" Foreground="{StaticResource Text}" FontSize="11.5" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </Border>

        <!-- list -->
        <ScrollViewer x:Name="ListScroll" VerticalScrollBarVisibility="Auto" Padding="16,10,10,14">
            <StackPanel x:Name="List"/>
        </ScrollViewer>
    </DockPanel>
</Window>
